---
name: spotify-genre-recommender
description: 使用 Spotify 官方插件按指定风格和数量推荐单曲，跨批次排除已推荐录音，通过 ncm-cli 自动匹配并保存网易云歌单；首次使用时引导安装或配置 Spotify 与 ncm-cli，确认后持久化就绪状态，正常调用不重复检验。缺少风格或数量时询问；曲目逐行输出“作者 - 歌曲名”，附插件要求的链接。用于 Spotify 风格推荐和保存其结果，不用于控制播放。
---

# Spotify 风格推荐

## 默认约定

- 复用本任务已确认的风格和正整数数量，只询问缺失字段。
- **用户已确认的长期偏好（2026-09-08）**：风格和数量明确后，自主选择检索词、艺人及具体作品，使用 `{track}` 后缀、换方向、重试、核验并补缺；以后无需用户再次确认这些检索决定，也不要求用户逐条复述查询词。此偏好限于推荐任务内的检索，不扩大其他操作权限，仍遵循工具实际支持的查询规则。
- **完成条件**：正常流程必须选满用户指定数量、通过作品级风格核验且跨历史及本批录音去重后，再交付最终清单。不足额的结果只能作为进度；某次只返回歌单、重复曲或不合格作品时，退出该分支并继续有效方向，不把首轮结果当作任务结束。真实工具限制或服务故障按下文保留进度并报告，不用重复确认检索词代替处理障碍。
- **推荐数量优先**：指定 10 首即推荐 10 首；网易云缺失则报告，不偷偷换歌。只有用户明确要求“网易云存满 N 首”才启用 save 模式。
- 默认自动保存网易云；本次明确“仅推荐／不保存”则跳过保存。编辑、审查技能不自动触发推荐或建单。
- 用 `setup-status` 读取本机已确认依赖；这是缓存读取，不是外部检验。所需组件已确认时直接运行，不检查 Spotify 工具、不运行 `doctor`；仅首次未确认、实际依赖错误、配置变化或用户明确要求时按[配置与恢复](references/setup.md)处理。
- 歌单名称用用户指定名称，否则本地日期 `YYMMDD 风格`。保留已有同名歌单，新建时自动加后缀。
- 测试使用独立状态目录和 `--test`，禁止写入真实歌单或正式推荐历史。

## 执行流程

先用 `setup-status` 读取缓存；只有所需组件未确认或发生依赖错误时才完整阅读[首次配置与故障恢复](references/setup.md)。随后完整阅读 [批次与命令](references/workflow.md) 和 [风格审查](references/genre-review.md)；需要保存时再完整阅读 [网易云保存](references/netease-save.md)。命令均通过 `scripts/workflow.py` 执行，不另写临时匹配、历史写入或建单脚本。

0. **依赖门禁**：读取 `setup-status`；Spotify 已确认且本次只推荐，或 Spotify/ncm 均已确认时立即继续。不要重复外部检查。缺少所需标记或实际调用出现依赖错误时才按[配置与恢复](references/setup.md)处理；已有批次保持原样。
1. **恢复或开批次**：用户说“继续”时复用原 batch；否则新建。先用 `plan` 读取缺额、未推荐的缓存单曲和低效查询记录。优先 `reuse` 导入同风格缓存再审查／选中，不只复用艺人名字。历史损坏时停止，不能绕过去重。
2. **首轮采用已验证的单曲检索写法**：缓存不足时，允许用 `风格 {track}` 检索，冷启动默认采用此写法；有已确认的同风格艺人线索也可用 `艺人名 {track}`，有具体目标作品时用 `艺人 - 曲名`。花括号保留并放在末尾。允许标题匹配的实际单曲进入候选池，不因曲名等于风格词而排除或立即换路；最终推荐仍须逐曲核验风格并去重。按[首轮检索规则](references/genre-review.md#首轮检索规则)执行。不要先发泛推荐、前置 `{track}`、裸 `track` 或多主体拼接请求。最终曲目元数据必须来自插件实际返回或可追溯缓存；`{track}` 是提示后缀，不是硬类型过滤保证。
3. **先去重再核验**：每轮至多并行 2 个明确方向，响应完整交给 `ingest`，艺人查询附已知 artist URI。随后先执行 `plan`，只对其待审窗口中的可用录音核验风格，再 `assess → select → plan`。不要对原始响应中的历史重复曲、同录音跨专辑版本或身份错配项先做网页研究。待审同录音只保留一个，其他版本仍留在候选库；真正的 Remix 等版本单独审查。
4. **按缺额换方向**：`remaining` 来自已原子选中的不同录音数，不用返回实体数、候选数或网易云匹配数代替。按 `select → review → reuse → discover` 推进；需要发现时检查 `avoid_queries` 和历史低产查询。只有歌单、重复或已无可核验候选时换艺人或具体作品，不重复原查询碰运气，不向短查询添加数量、“songs”或“排除已推荐”等长句来补缺。去重由本地流程完成。每轮只选择有依据的下一方向，不预设每位艺人的搜索配额；已满 N 立即进入保存／交付。真实工具限制、鉴权、限流或服务故障保留同批进度并一次说明，不能把普通无效分支当成整项任务结束。
5. **原子选中**：`select` 在同一事务中保存批次与历史预留，跨专辑同录音只选一次。10 首默认每位主艺人至多 2 首；这是可有据覆盖的软限制，不以牺牲风格贴切度换多样性。存满模式先匹配，再选中不同网易云 ID 的曲目。
6. **匹配与保存**：`search → resolve（仅歧义项）→ export`。推荐模式默认仅匹配已选中的 N 首，避免预查落选候选；存满模式才提前匹配。只按 ID 集合读回核实，不比较或调整曲序。失败保留同批次和歌单 ID，恢复前先对账。
7. **交付**：`render` 持久保存 ready 清单后输出。ready 不冒充 delivered；只有存在实际交付确认才标记 delivered。中断后重发同批清单，不再当成新的推荐，也不释放其预留。

## 输出

曲目部分每首独占一行，仅显示 `作者 - 歌曲名`，不编号、不加理由。遵循当前 Spotify 工具链接要求；需要链接时将整行做成 `[作者 - 歌曲名](插件返回的链接)`，不得拼接 Spotify URL。若用户要求严格无链接纯文本而插件要求链接，简短说明冲突。

正常完成恰好 N 首。保存后另加一句 `已保存 X/N 首到 [歌单名](网易云链接)`，X 必须是实际读回确认的不同歌曲数量。列明未匹配、写入失败或未核实的歌曲；API 成功不等于核实成功，存入也不保证可播放。仅推荐模式只输出曲目；真实障碍导致未满额时明确未完成。

## 维护验证

运行 `python3 -m unittest discover -s <skill-dir>/tests -v`。离线用例覆盖完整 10 首流程、跨批去重、身份与风格门槛、并发预留、缓存、超时恢复和集合核对。真实测试另建隔离 `--test` 批次，只做 Spotify 检索和网易云搜索；明确区分实测与模拟，不因合成用例通过就宣称风格效果已验证。
