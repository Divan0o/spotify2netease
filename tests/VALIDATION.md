# 2026-09-06 验证记录

## 配置确认缓存与按需复检（当前规则）

Spotify 插件与 ncm-cli 在首次确认就绪后分别写入本地 SQLite 配置状态。正常调用只读取这两个本地标记，不再枚举 Spotify 工具，也不再运行 `ncm-cli config get`、登录探测或 `doctor`。确认没有有效期，不做定时刷新。

只有首次尚未确认、真实调用返回依赖／鉴权错误、用户更换账户或 API 配置、或用户明确要求复检时，才重新进入对应依赖的检测与引导。Spotify 故障只清除 Spotify 标记；ncm-cli 故障经按需 doctor 确认未就绪后只清除 ncm 标记。新增2项回归覆盖跨 Store 实例持久化、按组件清除、全量清除和旧数据库缺少 `setup` 字段时的兼容迁移；完整离线回归现为42项。

## Spotify 与 ncm-cli 初版统一启动预检（已被缓存规则替代）

将 Spotify 插件和 ncm-cli 的依赖处理集中到 `references/setup.md`，并要求在询问风格／数量、创建或恢复批次之前完成。Spotify 插件以当前会话中存在明确属于 Spotify 且支持搜索／推荐的可调用工具为准，不把本机 Spotify 客户端或网页访问当成插件；缺失时分别给出桌面／网页插件目录和 Codex CLI `/plugins` 引导，并提醒安装后开启新会话。IDE 扩展不支持插件时引导改用桌面端 Codex 或 CLI。

ncm-cli 的 doctor、安装、AppId／PrivateKey 和登录引导一并移动到统一预检；本次明确仅推荐时可跳过 ncm-cli，但不能跳过 Spotify。当前会话工具清单已确认 Spotify 搜索能力来自 `Spotify` 插件；OpenAI 官方插件文档已核对安装、连接和新会话要求。代码未变更，40项离线回归与技能格式校验继续通过。

## ncm-cli 配置预检与引导

新增无状态、只读的 `workflow.py doctor`：分别报告 ncm-cli 缺失、无法执行、AppId／PrivateKey 缺失、未登录和 ready。返回内容不包含配置值或网易云账户 ID；缺少凭据时只返回占位命令和开放平台申请地址。主流程仅在需要网易云匹配或保存时预检，只推荐模式跳过；未就绪时保留已有批次并引导配置，复检 ready 后才继续。

新增4项离线回归覆盖未安装、缺少 PrivateKey、未登录和已就绪状态；完整回归现为40项。另在当前已配置环境运行真实只读 doctor，未创建歌单或修改网易云数据。技能格式校验继续使用临时虚拟环境中的 PyYAML，不修改全局 Python。

## 用户调整：保留风格后缀与标题匹配（当前规则）

按用户明确要求，保留 `风格 {track}` 为正常检索入口，冷启动计划改为 `genre_track_suffix`。标题匹配的实际单曲进入候选池与审查窗口，不因 `title_hits` 被自动排除或触发换路；最终选中仍需作品级风格依据及录音去重。缓存优先、已确认艺人的后缀检索和具体作品检索继续可用。

扩展现有标题匹配回归：同名标题进入 review，有作品级评估后可 select；单凭标题不能直接选中。以下后缀实测记录中的“不默认使用纯风格查询”是本次用户调整前的策略。

## `{track}` 后缀对照验证

用户明确要求验证 `{track}` 并在有效时采用。完成10次真实只读 Spotify 搜索，统一 `language=zh-CN`；隔离目录 `/tmp/spotify-track-token.dEC0PQ`，测试批次 `20260906T153950-eff5d259`。完整原始响应为 `response-0.json` 至 `response-9.json`，查询、类型统计与 URI 对照在 `summary.json`，所有响应已通过 workflow ingest 保存到隔离数据库。临时目录可能被系统清理。没有选中推荐、写入正式历史或调用网易云。

| prompt 原文 | 次数 | 每次实体统计 |
| --- | --- | --- |
| `为我推荐10首deep house歌曲` | 1 | 5 playlist |
| `为我推荐10首deep house歌曲 {track}` | 1 | 5 playlist |
| `{track} deep house` | 1 | 5 playlist |
| `deep house` | 1 | 5 playlist |
| `deep house {track}` | 2 | 5 track |
| `deep house track` | 1 | 5 playlist |
| `Kerri Chandler {track}` | 2 | 5 track，artist URI 全部匹配 |
| `Kerri Chandler` | 1 | 3 track、1 artist、1 playlist |

两次 `deep house {track}` 返回相同5个URI，其中4首标题为 Deep House，3首是同艺人同名同长的跨发行重复；没有据此接受任何曲风结论。两次艺人后缀查询均为5首单曲，末位曲目发生变化，因此只报告类型观察，不承诺排序或完整结果固定。对照支持短查询末尾保留花括号的 `{track}` 改善当前插件实体类型，不能证明这是官方硬过滤语法或所有查询均有效。

据此将已确认艺人线索的发现入口改为 `artist_track_suffix`，保留缓存优先与无可靠身份时的 `exact_track_lookup`。没有将纯风格后缀查询设为默认，以避免把标题命中当成曲风推荐；无合格产出时回退具体作品。36项离线回归与技能格式校验通过。此前“无缓存统一精确查询”的记录是本次优化前的历史版本。

## Deep House 首轮精确作品检索修正

依据本任务上一轮实际工具响应：泛推荐、两次艺人加风格查询、两次五首作品拼接查询均返回歌单；风格加“track／不要歌单”返回5首同名标题命中。随后10次单首 `艺人 - 曲名` 查询均返回目标单曲，部分响应仍包含歌单或专辑。本次修改复用这些已有观测，没有重新发起 Spotify 推荐或网易云写入。

首轮规则改为缓存优先、缺额时单首精确作品检索，并同步 `plan.entry_route`：无缓存时统一为 `exact_track_lookup`。删除冷启动泛风格入口和多首拼接建议；每轮最多两次查询，处理并选中本轮结果后再补缺。只审查单曲，不保证服务端响应排除所有歌单。

36项离线回归全部通过，新增用例覆盖冷启动精确检索入口、有缓存时仍优先复用、缓存已推荐但保留艺人线索时仍采用精确检索，以及计划读取不修改状态。技能格式检查通过，PyYAML 安装在临时虚拟环境中。离线测试验证本地行为；检索效果依据上一轮实测，未宣称新规则已完成新一轮线上验证。

## Afro house 首轮路由与候选缓存优化

正式批次 `20260906T100834-1d0fe51b` 的7次Spotify调用中，首轮泛推荐返回5个歌单、0首单曲；Da Capo查询返回5首身份不符的作品，其余5次艺人查询各选2首。逐艺人搜索加每艺人2首的选择上限抬高了调用次数；此前plan只复用艺人线索，没有导入未推荐单曲的操作。

本次2次只读对照保存在 `/tmp/spotify-route-check.DBjQ8k`（系统可能清理临时目录），隔离测试批次 `20260906T102342-7bb1fc2b`：

- “推荐10首……具体单曲，不返回歌单”仍返回5个playlist。
- “搜索afro house风格的歌曲，结果类型为单曲（track），不要歌单”返回5个Song，其中3首标题恰为Afro House。这里只验证实体类型，未把它们认定为风格合格或推荐给用户。

所以移除推荐式首问探路，使用缓存优先和已知低效查询提示；不能承诺靠改写提示彻底修复Spotify服务端路由。正式历史目前可找出12首未推荐、未被否决的缓存候选，均仍待作品级审查，不保证12首均合格。

新增reuse命令保留Spotify元数据、评估和来源链，排除录音冲突／测试源／异风格／不确定与否决项，不复制网易云匹配或歌单状态。35项离线回归全部通过，含模拟10首缓存推荐零新增Spotify调用、未审候选不自动接受、原子冲突回滚、旧评估不覆盖较新批次疑义与历史查询记忆。没有重新推荐、预留正式历史或创建网易云歌单；缓存零调用结果是模拟用例，不冒充新一轮真实推荐验收。技能格式校验使用临时目录内的PyYAML依赖，不改全局Python环境。

## 检索效率改进验证

Jersey club 正式批次日志确认23次Spotify调用、74首唯一候选、10次网易云搜索；后者全部一次命中，优化重点为候选发现与核验时机。新增只读plan命令，实际可从该批次提取7个主艺人线索，优先返回历史直接检索有合格产出的艺人；不复制旧推荐。

优化后的28项回归测试全部通过，包括同风格线索复用、隔离测试/历史保护、先审查再搜索的行动提示、有限候选窗口、满额停止发现、推荐模式只查已选中曲目及保存模式预查。未以真实Spotify重跑新一轮推荐，因此不承诺固定的查询降幅；查询分支裁剪和候选取舍仍需执行者遵循指引。

- 23 项标准库 unittest 回归全部通过；技能 quick_validate 通过。
- 完整 10 首闭环使用合成实体与 FakeClient：检索匹配、事务选中、模拟创建、模拟添加、倒序读回、交付准备和重复恢复。未调用真实写接口。
- 默认 recommend 模式的 10 首推荐、9 首网易云匹配用例通过，保持10首原推荐，不替换缺歌。
- 并发去重、跨专辑录音去重、possible 版本拦截、身份错配、风格证据结构、拒绝缓存、主艺人软限制、保存数模式、准备/交付阶段、取消预留、业务码错误和 shell 字符安全均有测试。
- 创建超时后唯一歌单恢复、部分添加后只补缺失、读回失败不盲写、测试批次拒绝写入均通过模拟验证。

## 真实只读抽测

隔离状态目录：`/tmp/spotify-skill-live.01lGci`，批次 `20260906T031631-90c0ad06`。原始 Spotify 返回与风格评估保存在该目录，网易云结果保存在隔离数据库；临时目录将来可能被系统清理。

首次自然语言请求返回5个playlist，ingest接受0首。明确单曲后返回5个Song，其中4首标题等于风格名；未将这4首自动认定为风格合格。

对 [Mc João, DJ R7 - Baile de Favela](https://open.spotify.com/track/5nwkZfrwFGehnwYA1Z664w?utm_source=openai&utm_medium=chatgpt) 使用 [Gshow 的 MC João 演出报道](https://gshow.globo.com/tv/noticia/atracao-do-ding-dong-mc-joao-fala-sobre-sucesso-do-hit-baile-de-favela.ghtml) 核验作品风格。ncm-cli 0.1.7真实搜索一次后，自动匹配同名、完整艺人一致、同专辑且时长差48ms的版本；未选择返回的混音或现场版。

本次真实测试是1首匹配与失败模式抽测，不是10首真实推荐验收，也未创建真实测试歌单；完整10首写入闭环仅模拟验证。规则能拦截无依据收歌，但不能保证 Spotify 的检索路由改变，主观风格贴切度仍需逐曲审查。

## 历史保护

原30首历史导入事务数据库，正式新增推荐0首。recommended.json 原文未修改，SHA-256 前后一致：`5e248c2fd34cda551c14925d02037c9398bd785bd3bdb5742ebf751a98e0381a`。原网易云歌单未修改；旧 exports 保留。
